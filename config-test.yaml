model:
  name: "ResNet50"
  num_classes: 10
  pretrained: true

training:
  batch_size: 128
  learning_rate: 0.01
  epochs: 50

data:
  train_dataset: "${TRAIN_DATASET_PATH}"
  test_dataset: "${TEST_DATASET_PATH}"
  shuffle: true

logging:
  log_file: "log.txt"
  verbose: true

