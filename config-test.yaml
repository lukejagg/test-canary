model:
  name: "ResNet101"
  num_classes: 10
  pretrained: true

training:
  batch_size: 64
  learning_rate: 0.001
  epochs: 50

data:
  train_dataset: "train_data.csv"
  test_dataset: "test_data.csv"
  shuffle: true

logging:
  log_file: "log.txt"
  verbose: false